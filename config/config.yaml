# 服务器配置
server:
  port: 8080
  mode: "debug"

# Redis 配置
redis:
  host: "localhost"
  port: 6379
  password: "123456"
  db: 0

# MySQL 配置
mysql:
  # 如果你想直接使用完整 DSN，可以填这里；留空时会使用下面的 host/port/user/password/dbname 生成。
  dsn: ""
  host: "localhost"
  port: 3306
  user: "game_app"
  password: "123456"
  dbname: "game_db"
